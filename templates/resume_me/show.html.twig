{% extends 'base.html.twig' %}

{% block title %}ResumeMe{% endblock %}

{% block body %}
    <h1>ResumeMe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resume_me.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ resume_me.firstname }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ resume_me.image }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ resume_me.updatedAt ? resume_me.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resume_me_index') }}">back to list</a>

    <a href="{{ path('resume_me_edit', {'id': resume_me.id}) }}">edit</a>

    {{ include('resume_me/_delete_form.html.twig') }}
{% endblock %}
