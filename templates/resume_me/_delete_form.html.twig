<form method="post" action="{{ path('resume_me_delete', {'id': resume_me.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resume_me.id) }}">
    <button class="btn">Delete</button>
</form>
