{% extends 'base.html.twig' %}

{% block title %}ResumeMe index{% endblock %}

{% block body %}
    <h1>ResumeMe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for resume_me in resume_mes %}
            <tr>
                <td>{{ resume_me.id }}</td>
                <td>{{ resume_me.firstname }}</td>
                <td>
                    <a href="{{ path('resume_me_show', {'id': resume_me.id}) }}">show</a>
                    <a href="{{ path('resume_me_edit', {'id': resume_me.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('resume_me_new') }}">Create new</a>
{% endblock %}
